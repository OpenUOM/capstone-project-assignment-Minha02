{
  "name": "capstone-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
<<<<<<< HEAD
    "pretest": "rm -rf test.sqlite && NODE_ENV=test knex migrate:unlock && NODE_ENV=test knex migrate:latest && NODE_ENV=test knex seed:run",
    "test": "NODE_ENV=test jest --config jest.config.js --runInBand --detectOpenHandles --forceExit",
    "test:specific": "NODE_ENV=test jest -t",
=======
    "test": "export NODE_ENV=test && forever start --minUptime 30000 ../backend/index.js && ng serve --port 4401 --configuration=test && sleep 15 && testcafe chrome:headless tests/ --page-load-timeout 60000 --assertion-timeout 10000 --selector-timeout 10000",
>>>>>>> parent of a1bf119 (angular)
    "start": "node backend/index.js",
    "delete-db": "rm -f db.sqlite test.sqlite",
    "migrate": "npm run delete-db && knex migrate:latest",
    "seed": "knex seed:run",
    "db": "npm run migrate && npm run seed"
  },
  "author": "keetmalin.13@cse.mrt.ac.lk",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.17.3",
    "forever": "^4.0.3",
    "knex": "^2.1.0",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "@angular/cli": "^19.1.4",
    "cross-env": "^7.0.3",
    "del-cli": "^4.0.1",
    "jest": "^27.5.1",
    "supertest": "^6.2.2",
    "wait-on": "^8.0.2"
  }
}
